apiVersion: apps/v1
kind: Deployment
metadata:
  name: jsapp
  labels:
    app: jsapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jsapp
  template:
    metadata:
      labels:
        app: jsapp
    spec:
      containers:
      - name: jsapp
        image: huxlee/jsapp:v1.29
        ports:
        - containerPort: 9191